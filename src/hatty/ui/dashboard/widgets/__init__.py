# hatty — MIT License. See LICENSE file for details.
"""Per-widget-type dashboard slot renderers. `base.py` holds the shared
`EntitySlotWidget` base class and the `WIDGET_FACTORIES` widget_type ->
factory registry; `visuals.py` holds shared pure rendering helpers reused
across widget types."""
